#!/usr/bin/env python3
str_obj = str(input("Enter an input? "))
def removed_one(str_obj):
    while True:
        str_len = len(str_obj)
        remove_one = 1
        check = (str_len-remove_one)
        print(check)
        str_obj in range(check)
        break
    return str_obj
if len(str_obj)==1:
    print(str_obj)
else:
    print(removed_one(str_obj))